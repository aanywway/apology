# Сайт-извинение ❤️

Статичный SPA на React + Vite + TypeScript + Tailwind CSS + Framer Motion + lucide-react.

## Запуск

```bash
npm install
npm run dev
```

Сборка:

```bash
npm run build
```

Имя в подписи письма заменяется в `src/components/LetterScreen.tsx` — строка `Твой [имя]`.
